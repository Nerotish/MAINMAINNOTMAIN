getgenv().SecureMode = true

print("Whitelisted")

 Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()
local Window = Rayfield:CreateWindow({
	Name = "Ohio Hub",
	LoadingTitle = "Ohio Hub",
	LoadingSubtitle = "By Rise And Akira",
	ConfigurationSaving = {
		Enabled = true,
		FolderName = "FameHub", -- Create a custom folder for your hub/game
		FileName = "FameHub"
	},
        Discord = {
        	Enabled = true,
        	Invite = "D6FmJdRX", -- The Discord invite code, do not include discord.gg/
        	RememberJoins = true -- Set this to false to make them join the discord every time they load it up
        },
	KeySystem = true, -- Set this to true to use our key system
	KeySettings = {
		Title = "FameHub",
		Subtitle = "Key System",
		Note = "Join the discord (https://discord.gg/D6FmJdRX)",
		FileName = "FameHub",
		SaveKey = true,
		GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
		Key = "Loser"
	}
})

Rayfield:Notify({
    Title = "Whitelisted",
    Content = "Script Loaded, Version: PAID",
    Duration = 10,
    Image = 4483362458,
    Actions = { -- Notification Buttons
        Ignore = {
            Name = "Close",
            Callback = function()
                print("Notification Prompt Closed")
            end
		},
	},
})

print("Script Loaded, Version: PAID")

local Tab = Window:CreateTab("Main", 4483362458)

local Section = Tab:CreateSection("Data Main")

local Button1 = Tab:CreateButton({
	Name = "Data Dupe",
	Callback = function()
		for i,v in pairs(game:GetService("ReplicatedStorage").API:GetChildren())do
    if v:IsA("RemoteEvent")then
        v:FireServer(workspace)
    end
end
	end,
})
